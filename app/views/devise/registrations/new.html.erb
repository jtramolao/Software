<!DOCTYPE html>
<html>
<head>
  <title>Reg&iactute;strate</title>
  <link href="http://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link href='https://fonts.googleapis.com/css?family=Lato:400,300' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" type="text/css" href="css/style.css" media="screen,projection">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
<body>
<div class="container">
  <div class="row">
    <div class="blue-grey darken-3">
      <h5 class="center login-title" style="color: #FFBA22">Crea una cuenta
      </h5>
    </div>
  </div>
<%if user_signed_in?%>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <div>
    <label>Ingresa tus datos para poder crear tu cuenta y acceder</label>
  </div>

    <div class="input-field col s12">
    <i class="material-icons prefix">email</i>
    <label for="email">Registra un email</label>
    <%= f.email_field :email, autofocus: true, :maxlength => 30 %>
            </div>
    <div class="input-field col s6">
      <i class="material-icons prefix">mode_edit</i>
      <label for="password">Crea una contraseña (minimo de 6 caracteres) </label>
      <%= f.password_field :password, autocomplete: "off" %>
    </div>


    <div class="input-field col s6">
      <i class="material-icons prefix">mode_edit</i>
      <label for="password">Confirma la contraseña</label>
      <%=f.password_field :password_confirmation, autocomplete: "off"%>
    </div>
      <div class="input-field col s6">
      <i class="material-icons prefix">report_problem</i>
      <label for="password">Ingresa contraseña de administrador para confirmar</label>
      <%=f.password_field :current_confirmation, autocomplete: "off"%>
    </div>

    <div class="col s12" style="color: #FFBA22">
      <%=f.submit "Crear cuenta" ,class:"btn center-align"  %>
    </div>
    <%= link_to 'Volver al inicio', welcome_index_path , class:"btn center-align"%>
</div>

<% end %>
<% else %>
  <div class="container">
    <label>Contacta al administrador para crear una cuenta</label>
                    <%= link_to 'Volver al inicio', welcome_index_path , class:"btn center-align"%>
  </div>

<% end -%>
</body>
</html>
