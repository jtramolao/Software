<% @workers.each do |worker| %>


       <tbody>
          <tr>
            <td><%= link_to worker.nombre, @worker.report %></td>
            <td><%= worker.rut %></td>
             <td> <% unless worker.equip.nil? %>

             <%= worker.equip.nombre %>

            <% else %>

            No Tiene Grupo

            <%end%>

          </td>
           
            <td><%= link_to "Eliminar", worker, method: :delete,class:"waves-effect waves-light btn" %></td>
          </tr>

        </tbody>



<%end%>

      </table>